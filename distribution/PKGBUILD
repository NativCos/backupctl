pkgname=backupctl
pkgver=1.0
pkgrel=1
pkgdesc="Backup to a remote location using Borg."
arch=("x86_64")
source=('backupctl::backupctl')
makedepends=('borg' 'openssh')
makedepends=('make')

package() {
  make DESTDIR="${pkgdir}" install
}
