pkgname=backupctl
pkgver=14
pkgrel=1
pkgdesc="Backup to a remote location using Borg."
arch=("x86_64")
source=('git+ssh://git@git.nkt.lan:222/nikita/backupctl.git#tag=v14')
sha256sums=('SKIP')
depends=('borg' 'openssh')
makedepends=('make')
backup=(etc/backupctl/{conf.json,excludefile})

package() {
  cd "$srcdir/$pkgname"
  make DESTDIR="${pkgdir}" install
}
